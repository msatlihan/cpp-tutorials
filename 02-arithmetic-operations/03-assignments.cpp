// Tutorials are coded by Musa Atlıhan with the help of Udacity C++ lessons.
// Credits https://www.udacity.com


#include "main.hpp"

// += operator: A += B equivalent of A = A + B
// -= operator: A -= B equivalent of A = A - B
// *= operator: A *= B equivalent of A = A * B
// /= operator: A /= B equivalent of A = A / B
// %= operator: A %= B equivalent of A = A % B

int main()
{
    
    return 0;
}
